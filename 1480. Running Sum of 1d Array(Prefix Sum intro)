class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int n=nums.size();
        int runningSum=0;
        for (int i=0;i<n;i++){
            runningSum+=nums[i];
            nums[i]=runningSum;
        }
        return nums;        
    }
};
